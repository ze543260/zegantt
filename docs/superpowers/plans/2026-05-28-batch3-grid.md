# Batch 3 — Grid & Sidebar Enhancements

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Progresso agregado nos grupos, sidebar redimensionável com drag handle, e skeleton loading state adequado.

**Architecture:** GanttGrid recebe progresso dos grupos computado em useGanttData. Sidebar usa useState para largura dinâmica em ProjectGantt.tsx. Skeleton substitui o spinner atual quando loading=true.

**Tech Stack:** React 18, TypeScript, CSS animations (shimmer), Vitest

---

## Files

- Modify: `src/hooks/useGanttData.ts` — expor groupProgress no retorno
- Modify: `src/components/GanttGrid/index.tsx` — progress bar nos grupos + skeleton
- Modify: `src/ProjectGantt.tsx` — sidebar width state + drag handle + skeleton wrapper
- Modify: `src/types.ts` — adicionar `sidebarWidth?: number`
- Modify: `src/zegantt.css` — keyframe shimmer

---

### Task 1: Computar progresso por grupo em useGanttData

**Files:**
- Modify: `src/hooks/useGanttData.ts`

- [ ] **Step 1: Ler useGanttData.ts e entender o retorno atual**

```bash
# Leia src/hooks/useGanttData.ts para entender o retorno
```

- [ ] **Step 2: Adicionar groupProgress ao retorno**

No final da função `useGanttData` (ou onde os dados são retornados), adicionar:

```typescript
// Calcula progresso médio por tipo (step/milestone/event/note) e por projectId
const groupProgress = useMemo(() => {
    const byType = new Map<string, { sum: number; count: number }>();
    const byProject = new Map<string, { sum: number; count: number }>();

    for (const task of tasks) {
        if (task.originalType !== 'step') continue; // progresso só para steps

        // Por tipo
        const typeKey = task.originalType;
        const typeEntry = byType.get(typeKey) || { sum: 0, count: 0 };
        typeEntry.sum += task.progress;
        typeEntry.count += 1;
        byType.set(typeKey, typeEntry);

        // Por projeto
        if (task.projectId) {
            const projEntry = byProject.get(task.projectId) || { sum: 0, count: 0 };
            projEntry.sum += task.progress;
            projEntry.count += 1;
            byProject.set(task.projectId, projEntry);
        }
    }

    const toPercent = (entry?: { sum: number; count: number }) =>
        entry && entry.count > 0 ? Math.round(entry.sum / entry.count) : null;

    return {
        byType: new Map([...byType.entries()].map(([k, v]) => [k, toPercent(v)!])),
        byProject: new Map([...byProject.entries()].map(([k, v]) => [k, toPercent(v)!])),
    };
}, [tasks]);

return { tasks, timeline, displayRows, taskRowIndex, arrows, criticalIds, delayedIds, relatedIds, groupProgress };
```

- [ ] **Step 3: Adicionar groupProgress ao GanttContextState em context/GanttContext.tsx**

```typescript
// src/context/GanttContext.tsx — adicionar ao interface GanttContextState:
groupProgress: {
    byType: Map<string, number>;
    byProject: Map<string, number>;
};
```

- [ ] **Step 4: Passar groupProgress no contextValue em ProjectGantt.tsx**

```typescript
// No contextValue useMemo, adicionar:
groupProgress: data.groupProgress,
```

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useGanttData.ts src/context/GanttContext.tsx src/ProjectGantt.tsx
git commit -m "feat: compute group progress averages in useGanttData"
```

---

### Task 2: Renderizar progress bar nos grupos do GanttGrid

**Files:**
- Modify: `src/components/GanttGrid/index.tsx`

- [ ] **Step 1: Adicionar groupProgress ao destructuring do useGanttContext**

```typescript
// src/components/GanttGrid/index.tsx — no useGanttContext():
const { ..., groupProgress } = useGanttContext();
```

- [ ] **Step 2: Atualizar a row de grupo (kind === 'group') para mostrar progress**

Dentro do render de `row.kind === 'group'`, substituir o conteúdo do flex container:

```tsx
<div style={{ display: 'flex', alignItems: 'center', gap: 8, flex: 1, minWidth: 0 }}>
    {row.collapsed
        ? <ChevronRight size={14} style={{ color: C.textSecondary, flexShrink: 0 }} />
        : <ChevronDown size={14} style={{ color: C.textSecondary, flexShrink: 0 }} />}
    <span style={{ fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.05em', color: C.textTitle, flexShrink: 0 }}>
        {t(`gantt.group.${row.groupType}`, row.label)}
    </span>
    <span style={{ fontSize: 10, fontWeight: 600, padding: '2px 6px', borderRadius: 9999, background: C.groupSoftStrong, color: C.textSecondary, flexShrink: 0 }}>
        {row.count}
    </span>
    {/* Progress bar para grupos de steps */}
    {row.groupType === 'step' && groupProgress.byType.has('step') && (
        <>
            <div style={{ flex: 1, height: 4, background: C.borderLight, borderRadius: 2, overflow: 'hidden', minWidth: 40 }}>
                <div style={{
                    width: `${groupProgress.byType.get('step')}%`,
                    height: '100%',
                    background: C.group,
                    borderRadius: 2,
                    transition: 'width 0.3s',
                }} />
            </div>
            <span style={{ fontSize: 10, fontWeight: 700, color: C.textSecondary, flexShrink: 0 }}>
                {groupProgress.byType.get('step')}%
            </span>
        </>
    )}
</div>
```

- [ ] **Step 3: Atualizar row de projectHeader para mostrar progress do projeto**

Dentro do render de `row.kind === 'projectHeader'`:

```tsx
{/* Após o span do projectTitle: */}
{groupProgress.byProject.has(row.projectId) && (
    <>
        <div style={{ flex: 1, height: 4, background: 'rgba(26,60,48,0.2)', borderRadius: 2, overflow: 'hidden', minWidth: 40 }}>
            <div style={{
                width: `${groupProgress.byProject.get(row.projectId)}%`,
                height: '100%',
                background: C.group,
                borderRadius: 2,
            }} />
        </div>
        <span style={{ fontSize: 10, fontWeight: 700, color: C.group, flexShrink: 0, marginRight: 4 }}>
            {groupProgress.byProject.get(row.projectId)}%
        </span>
    </>
)}
```

- [ ] **Step 4: Commit**

```bash
git add src/components/GanttGrid/index.tsx
git commit -m "feat: show aggregated progress in group and project header rows"
```

---

### Task 3: Sidebar redimensionável

**Files:**
- Modify: `src/types.ts`
- Modify: `src/ProjectGantt.tsx`
- Modify: `src/context/GanttContext.tsx`

- [ ] **Step 1: Adicionar sidebarWidth prop**

```typescript
// src/types.ts — em ProjectGanttProps:
/** Controlled sidebar width in px. Uncontrolled default reads from localStorage. */
sidebarWidth?: number;
```

- [ ] **Step 2: Adicionar estado de sidebar width em ProjectGantt.tsx**

```typescript
// Após os imports, adicionar:
const SIDEBAR_MIN = 200;
const SIDEBAR_MAX = 700;
const SIDEBAR_STORAGE_KEY = 'zg-sidebar-w';

// No corpo de ProjectGantt, antes dos outros states:
const [sidebarW, setSidebarW] = useState<number>(() => {
    if (props.sidebarWidth) return props.sidebarWidth;
    try {
        const stored = localStorage.getItem(SIDEBAR_STORAGE_KEY);
        return stored ? Number(stored) : LEFT_W;
    } catch {
        return LEFT_W;
    }
});

// Sincronizar com prop controlada:
useEffect(() => {
    if (props.sidebarWidth !== undefined) setSidebarW(props.sidebarWidth);
}, [props.sidebarWidth]);
```

- [ ] **Step 3: Adicionar drag handle entre GanttGrid e GanttChart**

No JSX de retorno de `ProjectGantt.tsx`, substituir o `<div style={{ display: 'flex', flex: 1, overflow: 'hidden'...}>`:

```tsx
<div style={{ display: 'flex', flex: 1, overflow: 'hidden', position: 'relative', background: C.surfaceAlt }}>
    {!props.hideSidebar && (
        <>
            {/* GanttGrid recebe largura dinâmica */}
            <div style={{ width: sidebarW, flexShrink: 0 }}>
                <GanttGrid />
            </div>
            {/* Drag handle */}
            <div
                style={{
                    width: 5,
                    flexShrink: 0,
                    background: 'transparent',
                    cursor: 'col-resize',
                    position: 'relative',
                    zIndex: 10,
                    transition: 'background 0.15s',
                }}
                onMouseEnter={e => { (e.currentTarget as HTMLElement).style.background = C.groupGlowSoft; }}
                onMouseLeave={e => { (e.currentTarget as HTMLElement).style.background = 'transparent'; }}
                onMouseDown={(e) => {
                    e.preventDefault();
                    const startX = e.clientX;
                    const startW = sidebarW;
                    const onMove = (ev: MouseEvent) => {
                        const next = Math.min(SIDEBAR_MAX, Math.max(SIDEBAR_MIN, startW + ev.clientX - startX));
                        setSidebarW(next);
                        try { localStorage.setItem(SIDEBAR_STORAGE_KEY, String(next)); } catch {}
                    };
                    const onUp = () => {
                        document.removeEventListener('mousemove', onMove);
                        document.removeEventListener('mouseup', onUp);
                    };
                    document.addEventListener('mousemove', onMove);
                    document.addEventListener('mouseup', onUp);
                }}
            />
        </>
    )}
    <GanttChart />
</div>
```

> **Nota:** `GanttGrid` usa `LEFT_W` de constants.ts internamente. Substituir a ref a `LEFT_W` dentro de `GanttGrid/index.tsx` por receber a largura via prop ou via contexto. A abordagem mais simples é passar `sidebarW` pelo contexto.

- [ ] **Step 4: Expor sidebarW no contexto**

```typescript
// src/context/GanttContext.tsx — adicionar ao GanttContextState:
sidebarW: number;

// src/ProjectGantt.tsx — no contextValue:
sidebarW,
```

- [ ] **Step 5: Usar sidebarW em GanttGrid**

```typescript
// src/components/GanttGrid/index.tsx:
const { ..., sidebarW } = useGanttContext();

// No div raiz do componente, substituir `width: LEFT_W` por:
<div style={{ width: sidebarW, flexShrink: 0, ... }}>
```

- [ ] **Step 6: Commit**

```bash
git add src/types.ts src/ProjectGantt.tsx src/context/GanttContext.tsx src/components/GanttGrid/index.tsx
git commit -m "feat: resizable sidebar with drag handle and localStorage persistence"
```

---

### Task 4: Skeleton loading state

**Files:**
- Modify: `src/zegantt.css`
- Modify: `src/ProjectGantt.tsx`

- [ ] **Step 1: Adicionar keyframe shimmer ao CSS**

```css
/* src/zegantt.css — adicionar ao final: */
@keyframes zg-shimmer {
  0%   { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
.zg-skeleton {
  background: linear-gradient(90deg, #f0f0f0 25%, #e8e8e8 50%, #f0f0f0 75%);
  background-size: 200% 100%;
  animation: zg-shimmer 1.4s ease-in-out infinite;
  border-radius: 4px;
}
```

- [ ] **Step 2: Criar componente de skeleton loading em ProjectGantt.tsx**

Substituir o bloco `if (props.loading)` existente (linhas ~772-778):

```tsx
if (props.loading) {
    return (
        <div
            role="status"
            aria-label="Loading Gantt chart"
            aria-live="polite"
            style={{
                width: '100%', display: 'flex', flexDirection: 'column',
                background: 'var(--zg-surface, #fff)',
                borderRadius: 12, overflow: 'hidden',
                boxShadow: 'var(--zg-shadow-panel)',
                border: `1px solid var(--zg-border-light, #ECECEC)`,
                height: 'calc(100vh - 48px)', minHeight: 600,
                ...generateGanttTheme(props.theme),
            }}
        >
            {/* Header skeleton */}
            <div style={{ padding: '14px 18px', borderBottom: `1px solid var(--zg-border, #D9D9D9)`, background: 'var(--zg-header-bg, #F2F5F3)', display: 'flex', gap: 12, alignItems: 'center' }}>
                <div className="zg-skeleton" style={{ width: 140, height: 20 }} />
                <div style={{ flex: 1 }} />
                <div className="zg-skeleton" style={{ width: 80, height: 30, borderRadius: 8 }} />
                <div className="zg-skeleton" style={{ width: 200, height: 30, borderRadius: 8 }} />
                <div className="zg-skeleton" style={{ width: 120, height: 36, borderRadius: 8 }} />
            </div>
            {/* Body skeleton */}
            <div style={{ display: 'flex', flex: 1, overflow: 'hidden' }}>
                {/* Sidebar skeleton */}
                <div style={{ width: 460, flexShrink: 0, borderRight: `1px solid var(--zg-border, #D9D9D9)`, padding: '0 16px' }}>
                    {/* Column headers */}
                    <div style={{ height: 64, display: 'flex', alignItems: 'center', gap: 8, borderBottom: `1px solid var(--zg-border, #D9D9D9)` }}>
                        <div className="zg-skeleton" style={{ flex: 1, height: 12 }} />
                        <div className="zg-skeleton" style={{ width: 60, height: 12 }} />
                        <div className="zg-skeleton" style={{ width: 60, height: 12 }} />
                    </div>
                    {/* Row skeletons */}
                    {Array.from({ length: 8 }, (_, i) => (
                        <div key={i} style={{ display: 'flex', alignItems: 'center', gap: 10, height: 50, borderBottom: `1px solid var(--zg-border-light, #ECECEC)` }}>
                            <div className="zg-skeleton" style={{ width: 14, height: 14, borderRadius: 3, flexShrink: 0 }} />
                            <div className="zg-skeleton" style={{ width: `${45 + (i % 4) * 10}%`, height: 12 }} />
                            <div style={{ flex: 1 }} />
                            <div className="zg-skeleton" style={{ width: 50, height: 11 }} />
                            <div className="zg-skeleton" style={{ width: 50, height: 11 }} />
                        </div>
                    ))}
                </div>
                {/* Chart skeleton */}
                <div style={{ flex: 1, padding: 16, display: 'flex', flexDirection: 'column', gap: 8 }}>
                    <div style={{ display: 'flex', gap: 4, marginBottom: 8 }}>
                        {Array.from({ length: 8 }, (_, i) => (
                            <div key={i} className="zg-skeleton" style={{ flex: 1, height: 30, borderRadius: 4 }} />
                        ))}
                    </div>
                    {Array.from({ length: 8 }, (_, i) => (
                        <div key={i} style={{ height: 50, display: 'flex', alignItems: 'center' }}>
                            <div
                                className="zg-skeleton"
                                style={{
                                    marginLeft: `${(i * 17) % 35}%`,
                                    width: `${20 + (i % 5) * 8}%`,
                                    height: 26,
                                    borderRadius: 13,
                                }}
                            />
                        </div>
                    ))}
                </div>
            </div>
        </div>
    );
}
```

- [ ] **Step 3: Rodar build**

```bash
npm run build 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
git add src/zegantt.css src/ProjectGantt.tsx
git commit -m "feat: replace spinner with shimmer skeleton loading state"
```

---

### Task 5: Build e testes finais

- [ ] **Step 1: Rodar todos os testes**

```bash
npm run test
```
Expected: PASS

- [ ] **Step 2: Build final**

```bash
npm run build
```

- [ ] **Step 3: Commit final**

```bash
git add -A
git commit -m "feat(batch3): grid — group progress, resizable sidebar, skeleton loading"
```
