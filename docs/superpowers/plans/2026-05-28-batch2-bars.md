# Batch 2 — Task Bars & Dependency Arrows

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Labels fora de barras estreitas, setas de dependência coloridas por tipo, e lag visível nas setas SVG.

**Architecture:** Três features isoladas em GanttTaskBar e GanttArrows. ArrowPath precisa ser estendido com `depType` e `lag` para B2/B3. computeArrows em dependencies.ts passa esses campos.

**Tech Stack:** React 18, TypeScript, SVG, Vitest

---

## Files

- Modify: `src/utils/dependencies.ts` — estender ArrowPath com depType e lag
- Modify: `src/components/GanttTaskBar/index.tsx` — label fora de barras estreitas
- Modify: `src/components/GanttArrows/index.tsx` — cores por tipo + lag label
- Modify: `src/types.ts` — adicionar `showLabelOutside?: boolean`
- Modify: `src/utils/dependencies.test.ts` — testes de ArrowPath com depType

---

### Task 1: Estender ArrowPath com depType e lag

**Files:**
- Modify: `src/utils/dependencies.ts`
- Modify: `src/utils/dependencies.test.ts`

- [ ] **Step 1: Escrever teste**

```typescript
// src/utils/dependencies.test.ts — adicionar ao final:
import { computeArrows } from './dependencies';
import type { GanttDependency } from '../types';
import type { InternalTask } from '../types/internal';

describe('computeArrows depType and lag', () => {
  const makeTask = (id: string, start: Date, end: Date): InternalTask => ({
    id, name: id, start, end, progress: 0, originalType: 'step', deps: [],
  });

  it('includes depType in ArrowPath', () => {
    const tasks = [
      makeTask('a', new Date(2026,0,1), new Date(2026,0,10)),
      makeTask('b', new Date(2026,0,11), new Date(2026,0,20)),
    ];
    const dep: GanttDependency = {
      id: 'd1', predecessorId: 'a', predecessorType: 'STEP',
      successorId: 'b', successorType: 'STEP', type: 'SS', lag: 3,
    };
    const tl = { start: new Date(2026,0,1), end: new Date(2026,1,1), totalDays: 31,
      dayWidth: 20, totalWidth: 620, months: [], years: [], days: [],
      todayIndex: -1 };
    const rowIndex = new Map([['a', 0], ['b', 1]]);
    const arrows = computeArrows(tasks, [dep], tl as any, rowIndex);
    expect(arrows[0].depType).toBe('SS');
    expect(arrows[0].lag).toBe(3);
  });
});
```

- [ ] **Step 2: Rodar para confirmar falha**

```bash
npm run test -- dependencies
```
Expected: FAIL — `depType` não existe em ArrowPath

- [ ] **Step 3: Estender ArrowPath e computeArrows**

```typescript
// src/utils/dependencies.ts — atualizar interface ArrowPath:
export interface ArrowPath {
    predId: string;
    succId: string;
    path: string;
    headX: number;
    headY: number;
    depType: import('../types').DependencyType; // NEW
    lag: number;                                // NEW
}

// Na função computeArrows, trocar o objeto de retorno (última linha do map):
return {
    predId: pred.id,
    succId: succ.id,
    path: d,
    headX: sx - 6,
    headY: sy,
    depType: dep.type,  // NEW
    lag: dep.lag,       // NEW
};
```

- [ ] **Step 4: Rodar testes**

```bash
npm run test -- dependencies
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/utils/dependencies.ts src/utils/dependencies.test.ts
git commit -m "feat: ArrowPath carries depType and lag"
```

---

### Task 2: Label fora de barras estreitas

**Files:**
- Modify: `src/types.ts`
- Modify: `src/components/GanttTaskBar/index.tsx`

- [ ] **Step 1: Adicionar prop showLabelOutside em types.ts**

```typescript
// src/types.ts — dentro de ProjectGanttProps antes do último `}`:
/** When true (default), renders task name to the right of bars narrower than 55px */
showLabelOutside?: boolean;
```

- [ ] **Step 2: Adicionar label fora da barra no GanttTaskBar**

No bloco `if (task.originalType === 'step')` de `src/components/GanttTaskBar/index.tsx`:

```typescript
// Adicionar após a declaração de barY e antes do return:
const LABEL_THRESHOLD = 55;
const showOutsideLabel = (props.showLabelOutside !== false) && w < LABEL_THRESHOLD;

const outsideLabelColor = isCritical || isDelayed ? C.today : C.textPrimary;
```

Dentro do `return` do step, após o último `</div>` do bar principal e antes do `</>` de fechamento:

```tsx
{showOutsideLabel && (
    <span
        style={{
            position: 'absolute',
            left: x + w + 6,
            top: barY,
            height: BAR_H,
            display: 'flex',
            alignItems: 'center',
            fontSize: 11,
            fontWeight: 600,
            color: outsideLabelColor,
            whiteSpace: 'nowrap',
            pointerEvents: 'none',
            maxWidth: 120,
            overflow: 'hidden',
            textOverflow: 'ellipsis',
            opacity: isBarDimmed ? 0.15 : 1,
            transition: 'opacity 0.18s',
        }}
        title={task.name}
    >
        {task.name}
    </span>
)}
```

- [ ] **Step 3: Rodar build**

```bash
npm run build 2>&1 | head -30
```
Expected: sem erros

- [ ] **Step 4: Commit**

```bash
git add src/types.ts src/components/GanttTaskBar/index.tsx
git commit -m "feat: show task name outside narrow bars"
```

---

### Task 3: Setas de dependência coloridas por tipo

**Files:**
- Modify: `src/components/GanttArrows/index.tsx`

- [ ] **Step 1: Definir mapa de cores por depType**

```typescript
// src/components/GanttArrows/index.tsx — adicionar antes do return do componente:
const DEP_COLORS: Record<string, string> = {
    FS: C.group,                 // verde escuro — padrão, mais comum
    SS: C.event,                 // laranja
    FF: 'var(--zg-dep-ff, #7c3aed)', // roxo
    SF: 'var(--zg-dep-sf, #0369a1)', // azul
};
```

- [ ] **Step 2: Usar depType para colorir setas**

Substituir a linha `const col = on ? C.arrowHover : ...` por:

```typescript
const typeColor = DEP_COLORS[a.depType] ?? C.arrow;
const col = on
    ? C.arrowHover
    : isArrowHighlighted
        ? typeColor
        : typeColor === C.group ? C.arrow : typeColor; // não ativas ficam na cor do tipo mas mais suave
```

Simplificando — apenas usar a cor do tipo quando ativa:
```typescript
const baseColor = DEP_COLORS[a.depType] ?? C.arrow;
const col = on
    ? C.arrowHover
    : isArrowHighlighted
        ? baseColor
        : C.arrow;  // inativas voltam para cinza padrão
```

- [ ] **Step 3: Commit**

```bash
git add src/components/GanttArrows/index.tsx
git commit -m "feat: dependency arrows colored by type (FS/SS/FF/SF)"
```

---

### Task 4: Lag visível nas setas

**Files:**
- Modify: `src/components/GanttArrows/index.tsx`

- [ ] **Step 1: Adicionar lag label no SVG de cada seta**

O componente `GanttArrows` está dentro de um SVG (renderizado pelo pai em GanttChart). Adicionar ao `<g>` de cada seta:

```tsx
// Calcular ponto médio do path para posicionar o label:
// Para setas simples (mesma linha), midX = (px + sx) / 2
// Para setas com curva (L), usar headX - 30 como fallback seguro

{a.lag !== 0 && (
    <g>
        <rect
            x={a.headX - 40}
            y={a.headY - 10}
            width={32}
            height={14}
            rx={4}
            fill="var(--zg-surface, #fff)"
            stroke={col}
            strokeWidth={0.8}
            opacity={isArrowActive ? 1 : 0}
        />
        <text
            x={a.headX - 24}
            y={a.headY + 1}
            fontSize={8}
            fontWeight={700}
            fill={col}
            textAnchor="middle"
            opacity={isArrowActive ? 1 : 0}
            style={{ pointerEvents: 'none', userSelect: 'none' }}
        >
            {a.lag > 0 ? `+${a.lag}d` : `${a.lag}d`}
        </text>
    </g>
)}
```

- [ ] **Step 2: Rodar build**

```bash
npm run build 2>&1 | head -30
```
Expected: sem erros

- [ ] **Step 3: Commit**

```bash
git add src/components/GanttArrows/index.tsx
git commit -m "feat: show lag value on dependency arrows"
```

---

### Task 5: Testes e build final

- [ ] **Step 1: Rodar todos os testes**

```bash
npm run test
```
Expected: PASS

- [ ] **Step 2: Build final**

```bash
npm run build
```

- [ ] **Step 3: Commit final**

```bash
git add -A
git commit -m "feat(batch2): bars — outside labels, colored arrows, lag display"
```
